// type UserData = {
//     id: string;
//     avatar: string;
//     name: string;
//   }

//with interface keyword you can only define objects while with the type keyword you can also define other types
export interface UserData {
    id: string;
    avatar: string;
    name: string;
  }